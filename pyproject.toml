[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gc_core"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["core/src"]
